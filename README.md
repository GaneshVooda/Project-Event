# Project-Event

sudoyum ins Dov -y
sudo usradd docadmin
sudo passwd dockeradmin


 su - docadmin
 nano Docfile
   3lines

ctr d
sudo su
usermod -aG dock docadimn
group docadmin
sudo nano/etc/ssh/sshd_config
no-yes

sudo sysctl restat sshd
sudo seri Doc start
